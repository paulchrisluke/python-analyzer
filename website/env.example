# Better Auth Configuration
# For production: Set using wrangler secret put BETTER_AUTH_SECRET
# For local development: Use .dev.vars file
BETTER_AUTH_SECRET=your-secret-key-here
BETTER_AUTH_URL=http://localhost:8787

# Cloudflare D1 HTTP Driver Configuration
# Required for drizzle-kit migrations and database operations
# ⚠️  SECURITY: CLOUDFLARE_API_TOKEN should NEVER be stored as a Worker secret
# Only use in .env for local development and GitHub Secrets for CI/CD
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id
CLOUDFLARE_D1_DATABASE_ID=your-d1-database-id
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token

# D1 Database
# Automatically configured in wrangler.toml
# Database name: cranberry-auth-db

# Local SQLite Database Path (for development only)
# Path to the SQLite database file. Can be relative or absolute.
# Default: local.db (relative to project root)
DATABASE_PATH=local.db

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# Example: http://localhost:3000,https://yourdomain.com,https://app.yourdomain.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://127.0.0.1:3001

# Environment Setup:
# - Local Development: Create .dev.vars file with local values
# - Production: Set secrets using wrangler secret put
# - Environment variables are configured in wrangler.toml

# Test Environment Variables
# Copy this file to .env in the website root and update with your test user credentials
# DO NOT commit the .env file to version control

# Admin user credentials
ADMIN_EMAIL=admin@test.local
ADMIN_PASSWORD=test-admin-password-123
ADMIN_NAME=Test Admin User

# Buyer user credentials  
BUYER_EMAIL=buyer@test.local
BUYER_PASSWORD=test-buyer-password-123
BUYER_NAME=Test Buyer User

# Viewer user credentials
VIEWER_EMAIL=viewer@test.local
VIEWER_PASSWORD=test-viewer-password-123
VIEWER_NAME=Test Viewer User

# Note: These are example credentials for testing only
# Use secure, unique passwords for your actual test environment
# Ensure these users exist in your test database before running tests
